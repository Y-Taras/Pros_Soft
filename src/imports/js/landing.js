$(document).ready(function(){
  $('#landing-slider').bxSlider({
    auto: true
  });

  // $('.bx-wrapper').css('border', 'none');
  // $('#landing-slider').css('margin-top', '0');
  //
  // $( "#landing-slider li" ).each(function( index ) {
  //   this.style.width = parseInt(this.style.width) + 10 + 'px';
  // });

});
